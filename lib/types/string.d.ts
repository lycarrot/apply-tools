/**
 * 将数字转换成千分制显示
 * @param num 传入转换的数字
 * @param point  保留小数
 * @returns
 */
export declare function tranNumber(num: number, point?: number): string | 0;
/**
 *  在字符串中含有多个空格时，测出来的宽度会一样
 * @param {*} str
 * @param {*} font
 * @returns
 */
export declare function getTextWidth(str?: string, font?: number): number;
